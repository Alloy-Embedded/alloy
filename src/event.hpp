#pragma once

#include "runtime/dma_event.hpp"
#include "runtime/event.hpp"
#include "runtime/interrupt_event.hpp"

namespace alloy {

namespace event = runtime::event;
namespace interrupt_event = runtime::interrupt_event;
namespace dma_event = runtime::dma_event;

}  // namespace alloy
