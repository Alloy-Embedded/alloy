/// Auto-generated peripheral definitions for ESP32
/// Generated by Alloy Code Generator from CMSIS-SVD
///
/// Device:  ESP32
/// Vendor:  ESPRESSIF SYSTEMS (SHANGHAI) CO., LTD.
/// Family:  esp32
///
/// DO NOT EDIT - Regenerate from SVD if needed

#ifndef ALLOY_GENERATED_ESP32_PERIPHERALS_HPP
#define ALLOY_GENERATED_ESP32_PERIPHERALS_HPP

#include <cstdint>

namespace alloy::generated::esp32 {

// ============================================================================
// PERIPHERAL BASE ADDRESSES
// ============================================================================

namespace peripherals {
constexpr uintptr_t DPORT = 0x3FF00000;  // DPORT Peripheral
constexpr uintptr_t AES = 0x3FF01000;    // AES (Advanced Encryption Standard) Accelerator
constexpr uintptr_t RSA = 0x3FF02000;    // RSA (Rivest Shamir Adleman) Accelerator
constexpr uintptr_t SHA = 0x3FF03000;    // SHA (Secure Hash Algorithm) Accelerator
constexpr uintptr_t UART0 =
    0x3FF40000;  // UART (Universal Asynchronous Receiver-Transmitter) Controller 0
constexpr uintptr_t SPI1 = 0x3FF42000;     // SPI (Serial Peripheral Interface) Controller 1
constexpr uintptr_t SPI0 = 0x3FF43000;     // SPI (Serial Peripheral Interface) Controller 0
constexpr uintptr_t GPIO = 0x3FF44000;     // General Purpose Input/Output
constexpr uintptr_t GPIO_SD = 0x3FF44F00;  // Sigma-Delta Modulation
constexpr uintptr_t FLASH_ENCRYPTION = 0x3FF46000;  // FLASH_ENCRYPTION Peripheral
constexpr uintptr_t FRC_TIMER = 0x3FF47000;         // FRC_TIMER Peripheral
constexpr uintptr_t RTC_CNTL = 0x3FF48000;          // Real-Time Clock Control
constexpr uintptr_t RTC_IO = 0x3FF48400;            // Low-power Input/Output
constexpr uintptr_t SENS = 0x3FF48800;              // SENS Peripheral
constexpr uintptr_t RTC_I2C = 0x3FF48C00;  // Low-power I2C (Inter-Integrated Circuit) Controller
constexpr uintptr_t IO_MUX = 0x3FF49000;   // Input/Output Multiplexer
constexpr uintptr_t HINF = 0x3FF4B000;     // HINF Peripheral
constexpr uintptr_t UHCI1 = 0x3FF4C000;    // Universal Host Controller Interface 1
constexpr uintptr_t I2S0 = 0x3FF4F000;     // I2S (Inter-IC Sound) Controller 0
constexpr uintptr_t UART1 =
    0x3FF50000;  // UART (Universal Asynchronous Receiver-Transmitter) Controller 1
constexpr uintptr_t I2C0 = 0x3FF53000;      // I2C (Inter-Integrated Circuit) Controller 0
constexpr uintptr_t UHCI0 = 0x3FF54000;     // Universal Host Controller Interface 0
constexpr uintptr_t SLCHOST = 0x3FF55000;   // SLCHOST Peripheral
constexpr uintptr_t RMT = 0x3FF56000;       // Remote Control
constexpr uintptr_t PCNT = 0x3FF57000;      // Pulse Count Controller
constexpr uintptr_t SLC = 0x3FF58000;       // SLC Peripheral
constexpr uintptr_t LEDC = 0x3FF59000;      // LED Control PWM (Pulse Width Modulation)
constexpr uintptr_t EFUSE = 0x3FF5A000;     // eFuse Controller
constexpr uintptr_t NRX = 0x3FF5CC00;       // NRX Peripheral
constexpr uintptr_t BB = 0x3FF5D000;        // BB Peripheral
constexpr uintptr_t MCPWM0 = 0x3FF5E000;    // Motor Control Pulse-Width Modulation 0
constexpr uintptr_t TIMG0 = 0x3FF5F000;     // Timer Group 0
constexpr uintptr_t TIMG1 = 0x3FF60000;     // Timer Group 1
constexpr uintptr_t SPI2 = 0x3FF64000;      // SPI (Serial Peripheral Interface) Controller 2
constexpr uintptr_t SPI3 = 0x3FF65000;      // SPI (Serial Peripheral Interface) Controller 3
constexpr uintptr_t APB_CTRL = 0x3FF66000;  // APB (Advanced Peripheral Bus) Controller
constexpr uintptr_t I2C1 = 0x3FF67000;      // I2C (Inter-Integrated Circuit) Controller 1
constexpr uintptr_t SDHOST = 0x3FF68000;    // SD/MMC Host Controller
constexpr uintptr_t EMAC_DMA = 0x3FF69000;  // Ethernet DMA configuration and control registers
constexpr uintptr_t EMAC_EXT =
    0x3FF69800;  // Ethernet Clock, PHY type, and SRAM configuration registers
constexpr uintptr_t EMAC_MAC = 0x3FF6A000;  // Ethernet MAC configuration and control registers
constexpr uintptr_t TWAI0 = 0x3FF6B000;     // Two-Wire Automotive Interface
constexpr uintptr_t MCPWM1 = 0x3FF6C000;    // Motor Control Pulse-Width Modulation 1
constexpr uintptr_t I2S1 = 0x3FF6D000;      // I2S (Inter-IC Sound) Controller 1
constexpr uintptr_t UART2 =
    0x3FF6E000;  // UART (Universal Asynchronous Receiver-Transmitter) Controller 2
constexpr uintptr_t RNG = 0x60035000;  // Hardware Random Number Generator
}  // namespace peripherals

// ============================================================================
// MEMORY MAP
// ============================================================================

namespace memory {
constexpr uintptr_t FLASH_ENCRYPTION = 0x3FF46000;
}  // namespace memory

}  // namespace alloy::generated::esp32

#endif  // ALLOY_GENERATED_ESP32_PERIPHERALS_HPP
