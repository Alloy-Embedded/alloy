# {{ project }}

Generated by `alloy new` for [{{ display_name }}](https://github.com/Alloy-Embedded/alloy).

## Build

```bash
cmake --preset debug
cmake --build --preset debug
```

The generated `CMakePresets.json` already points at:

- `ALLOY_BOARD={{ board }}`
- `ALLOY_ROOT={{ alloy_root }}`{% if toolchain_bin %}
- toolchain bin: `{{ toolchain_bin }}`{% endif %}

## Flash and monitor

```bash
alloy flash --board {{ board }} --target {{ project }} --build-first
alloy monitor --board {{ board }}
```

## VS Code

Open this directory in VS Code. The generated `.vscode/` configures clangd against
`build/debug/compile_commands.json`, exposes build/flash/monitor as tasks, and provides a
debug launch configuration when a probe is available.
