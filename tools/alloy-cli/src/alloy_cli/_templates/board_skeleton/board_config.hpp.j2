#pragma once

// Generated skeleton hardware configuration for {{ mcu }}.
// Each TODO marks a value you must fill in for your board.

#include "device/runtime.hpp"

namespace {{ device }} {

// TODO: pick the LED pin and polarity used by your board.
// Example (replace with your hardware):
//   using led_green = alloy::device::pin<alloy::device::PinId::PA5>;
//   static constexpr bool led_green_active_high = true;

// TODO: pick a debug UART instance and pins (TX/RX) routed to your probe.

}  // namespace {{ device }}
