// Generated skeleton syscalls for bare-metal targets. Replace with your own
// implementations as needed.

#include <errno.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>

extern "C" {

int _close(int) { return -1; }
int _fstat(int, struct stat*) { errno = ENOSYS; return -1; }
int _isatty(int) { return 0; }
off_t _lseek(int, off_t, int) { return 0; }
ssize_t _read(int, char*, size_t) { return 0; }
ssize_t _write(int, const char*, size_t count) { return static_cast<ssize_t>(count); }
void _exit(int) { while (true) {} }
int _getpid() { return 1; }
int _kill(int, int) { errno = EINVAL; return -1; }

}
