// Generated by `alloy new` for {{ display_name }}.
// Replace with your application; see docs/COOKBOOK.md in the Alloy runtime for
// canonical patterns.

#include "../board/board.hpp"

int main() {
    board::init();

    // Once board/board.hpp exposes make_led() (mirrors the in-tree boards), the
    // following blink loop is a useful smoke test:
    //
    //   auto led = board::make_led();
    //   while (true) {
    //       led.toggle();
    //       for (volatile int i = 0; i < 1'000'000; ++i) { }
    //   }

    while (true) {
        // TODO: replace with your application loop.
    }
}
